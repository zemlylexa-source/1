ZENTORA PUBLIC BUILD

This package is a combined client + server deployment.

Render settings:
Root Directory: empty
Runtime: Docker
Dockerfile Path: ./Dockerfile
Build Command: empty
Publish Directory: empty
Start Command: empty

After deploy, open /api/health on the Render URL. It should return {"ok":true,...}.
Then open the main Render URL to use the messenger.
